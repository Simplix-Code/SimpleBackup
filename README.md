# SimpleBackup
SimpleBackUp is a lightweight, configurable backup tool for Linux. Define jobs in a simple JSON file and let it automatically create, zip, and manage backups on schedule. No databases, no cloud, no clutter — just clean, reliable automation that keeps your data safe with minimal setup.
